#include <bits/stdc++.h>
using namespace std;


int knap(vector<int> &arr,int n,int s){
	if()
}

int main(){
	int n,r,s;
	cin>>n>>r>>s;
	vector<int> arr(n);
	for (int i = 0; i < n; ++i)
	{
		cin>>arr[i];
	}
	int knap(arr,n,s);
}